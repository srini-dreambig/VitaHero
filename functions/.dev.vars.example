# Cloudflare Worker secrets (set in Rork dashboard or `wrangler secret put`)
# Copy to functions/.dev.vars for local worker testing — never commit real values.

DATABASE_URL=postgresql://user:pass@host/neondb?sslmode=require
PLIVO_AUTH_ID=
PLIVO_AUTH_TOKEN=
# Your purchased Plivo phone number in E.164 format (e.g. +12562828337)
PLIVO_SRC_NUMBER=

# Rork Toolkit — used server-side for AI diet tips (NOT in the Android app)
TOOLKIT_URL=https://toolkit.rork.app
TOOLKIT_SECRET_KEY=

# Closed-app admin import + invites
# ADMIN_API_KEY doubles as the login credential for the web admin portal
# (visit https://kidhero-health-backend.rork.app/admin and paste this key).
# It also lets you create admin accounts via POST /api/auth/signup with
# the X-Admin-Key header.
ADMIN_API_KEY=
# HMAC key used to sign stateless invite tokens (keep separate from ADMIN_API_KEY).
INVITE_SIGNING_KEY=
# Android App Links: comma-separated SHA-256 signing-cert fingerprints.
ANDROID_CERT_SHA256=
# Optional Play Store URL used as the install fallback on the invite landing page.
APP_PLAY_URL=

# ── DEV MODE (testing without a purchased Plivo number) ──────
# Set DEV_MODE=true to bypass Plivo SMS entirely:
#  • Any phone number can request an OTP (no admin provisioning needed).
#  • The OTP code is returned in the /phone/send response as dev_otp.
#  • A test profile is auto-provisioned on first OTP request.
#  • Invite SMS sends are skipped (the invite link is logged instead).
# NEVER enable in production — the OTP is exposed in the API response.
DEV_MODE=
